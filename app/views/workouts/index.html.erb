<div class="root_view">
  <div class="workouts_view center_column">
    <h1>Workouts</h1>

    <ul>
      <% @workouts.each do |workout| %>
        <li>
          <%= link_to workout.title, workout %>
        </li>
      <% end %>
    </ul>

    <%= link_to "New Workout", new_workout_path %>
  </div>
</div>